/**
 * Shared TypeScript types for menu data and widget state.
 *
 * Phase 0 stub — full type definitions land in Phase 1 per PRD §9, populated
 * from the schema in PRD §6 (Categories, Ingredients, MealFormats,
 * PortionOptions, PresetBowls).
 */

export {};
