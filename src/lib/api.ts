/**
 * Menu data fetch. Two modes controlled by `import.meta.env.VITE_API_URL`:
 *   - Dev / unset → returns the local seed JSON in `data/seed-ingredients.json`.
 *   - Production → fetches the deployed Cloudflare Worker (`GET /menu`).
 *
 * Phase 0 stub — real implementation in Phase 1 per PRD §9.
 */

export {};
