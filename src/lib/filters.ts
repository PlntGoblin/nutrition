/**
 * Diet + allergen filter logic. Pure: `applyFilters(ingredient, filters)
 * → boolean`. When a filter is active, non-matching cards desaturate to
 * 40% opacity rather than disappear (PRD §4.1 #7).
 *
 * Phase 0 stub — real implementation in Phase 5.
 */

export {};
