/**
 * Pure nutrition math — sum macros across format base + selected ingredients,
 * applying portion multipliers. Pure functions only — no side effects, no
 * store reads.
 *
 * Phase 0 stub. Phase 1 implements `calculateTotals(format, selections,
 * ingredients)` and the matching unit tests in `tests/nutrition.test.ts`.
 */

export {};
