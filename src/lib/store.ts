/**
 * Global widget state via Preact signals.
 *
 * Phase 0 stub. Phase 1 will define:
 *   - menuData (loaded from API)
 *   - selectedFormat
 *   - selections: Record<ingredientId, { portionMultiplier: number }>
 *   - activeFilters: { diets: string[], excludeAllergens: string[] }
 *
 * Compare-mode state intentionally absent — compare was demoted to P2 in
 * PRD v2.0.
 */

export {};
