/**
 * Portion stepper — None/Light/Regular/Extra/Double per ingredient (PRD §4.2 #15).
 * For Forefathers: applies primarily to Cheese (the menu's "Double Cheese +$1")
 * and any future ingredient where AllowsExtra = true.
 * Phase 0 stub — real implementation in Phase 7 per PRD §9.
 */
import type { JSX } from "preact";

export function PortionStepper(): JSX.Element {
  return <div data-component="PortionStepper" />;
}
