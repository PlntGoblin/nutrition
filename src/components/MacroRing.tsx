/**
 * Macro donut chart — animated SVG ring (protein/carbs/fat split).
 * Animates segment pathLength on macro changes (PRD §8.4).
 * Phase 0 stub — real implementation in Phase 3 per PRD §9.
 */
import type { JSX } from "preact";

export function MacroRing(): JSX.Element {
  return <svg data-component="MacroRing" />;
}
