/**
 * Ingredient card — photo, name, calorie count, allergen icons.
 * Tap to add/remove. See PRD §8.4 for visual spec.
 * Phase 0 stub — real implementation in Phase 2 per PRD §9.
 */
import type { JSX } from "preact";

export function IngredientCard(): JSX.Element {
  return <div data-component="IngredientCard" />;
}
