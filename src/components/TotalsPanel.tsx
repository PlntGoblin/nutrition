/**
 * Totals panel — calories + macros + macro donut + %DV bars.
 * Right-rail on desktop, bottom-sheet content on mobile (PRD §8.4).
 * Phase 0 stub — real implementation in Phase 2 (basic) and Phase 3 (polished).
 */
import type { JSX } from "preact";

export function TotalsPanel(): JSX.Element {
  return <div data-component="TotalsPanel" />;
}
