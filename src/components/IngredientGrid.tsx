/**
 * Ingredient grid — renders cards for the current category.
 * Phase 0 stub — real implementation in Phase 2 per PRD §9.
 */
import type { JSX } from "preact";

export function IngredientGrid(): JSX.Element {
  return <div data-component="IngredientGrid" />;
}
