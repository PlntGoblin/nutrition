/**
 * Popular Builds gallery — pre-built bowls shown on empty state (PRD §4.2 #16).
 * For Forefathers, the three menu salads (Buffalo Chicken, BBQ Chicken,
 * Steak Salad) are seeded as preset starting points the guest can customize.
 * Phase 0 stub — real implementation in Phase 7 per PRD §9.
 */
import type { JSX } from "preact";

export function PresetGallery(): JSX.Element {
  return <div data-component="PresetGallery" />;
}
