/**
 * Card primitive — 16 px radius, 1 px border, surface bg. PRD §8.4.
 * Phase 0 stub.
 */
import type { JSX } from "preact";

export function Card(props: JSX.HTMLAttributes<HTMLDivElement>): JSX.Element {
  return <div {...props} />;
}
