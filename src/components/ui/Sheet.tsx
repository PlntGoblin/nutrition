/**
 * Sheet primitive — bottom-sheet container with backdrop and drag handle.
 * Used by BottomSheet on mobile. Phase 0 stub.
 */
import type { JSX } from "preact";

export function Sheet(props: JSX.HTMLAttributes<HTMLDivElement>): JSX.Element {
  return <div {...props} />;
}
