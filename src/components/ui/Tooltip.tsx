/**
 * Tooltip primitive — used for ingredient descriptions and
 * filter-mismatch explanations on disabled cards. Phase 0 stub.
 */
import type { JSX } from "preact";

export function Tooltip(props: JSX.HTMLAttributes<HTMLDivElement>): JSX.Element {
  return <div {...props} />;
}
