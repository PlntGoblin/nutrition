/**
 * Mobile bottom sheet — collapsed (64 px) shows calories + protein + chevron;
 * expanded (80vh) shows full nutrition + share + filters. Drag-to-expand,
 * swipe-to-dismiss, backdrop blur (PRD §8.4 + §4.1 #5).
 * Phase 0 stub — real implementation in Phase 4 per PRD §9.
 */
import type { JSX } from "preact";

export function BottomSheet(): JSX.Element {
  return <div data-component="BottomSheet" />;
}
